You already imported my GitHub Pages project into Replit and set it up with a Python HTTP server so I can preview it. Please now do the following in one go:

### ✅ Step 1 — Confirm Current Workflow

* Verify that the current site loads correctly in Replit preview.
* Vendor can log in, add products, and see them listed.
* Customer can log in with predefined accounts (customer1/pass1, etc.) and see the dashboard.

### 🐞 Step 2 — Fix Existing Bugs

1. **Vendor Edit Bug**: Editing products sometimes doesn’t save properly — fix this so vendor edits update in the backend and UI consistently.
2. **Customer Purchase Bug**: When a customer purchases, quantity decrements but later resets — fix the data persistence so product quantities remain correct.
3. **Customer Product Visibility**: Ensure products added by vendor always show in the customer dashboard.
4. **Customer Expiry Alerts**: Change logic so customers only see expiry alerts for products they purchased that are expiring soon. Vendors should still see expiry alerts for all store products.

### 🎨 Step 3 — UI/UX Improvements

* Clean, modern, mobile-friendly design (responsive grid, better colors, icons for alerts).
* Tabbed navigation or cards to clearly separate Vendor vs Customer dashboards.
* Distinct warning/error/success styling for messages.
* Optional dark mode toggle.

### ✨ Step 4 — New Features

1. **Vendor Adds Customers**:

   * Vendor dashboard should include a form to create new customer accounts (username + password).
   * New accounts saved into the backend under `customers`.
   * Prevent duplicate usernames.

2. **QR Code Scanning**:

   * Add ability for customers to scan a product QR code with their mobile camera to autofill product ID for purchase.
   * Use a JS QR scanning library like `html5-qrcode`.

3. **Purchase History Export**:

   * Allow customers to export their purchase history as CSV.

---

### 🔥 Step 5 — Backend Migration to Firebase

* Replace JSONBin with **Firebase Realtime Database**.
* Use Firebase SDK (modular v9 API) to handle reads/writes instead of REST polling.
* Database schema:

  ```json
  {
    "products": {
      "prod1": { "id": "prod1", "name": "Milk", "quantity": 10, "expiryDate": "2025-10-10", "vendorPhone": "12345", "batchNo": "B001" }
    },
    "customers": {
      "customer1": "pass1",
      "customer2": "pass2"
    },
    "purchases": {
      "customer1": [ { "productId": "prod1", "productName": "Milk", "date": "..." } ]
    }
  }
  ```
* Always merge nodes when updating to avoid overwrites.
* Replace setInterval polling with Firebase’s `onValue` listeners for real-time sync.

---

### 📡 ESP32/Raspberry Pi Compatibility

* Use Firebase REST endpoints so ESP32/Raspberry Pi can also fetch/update product data.
* Keep API calls lightweight and documented.

---

### 🧪 Step 6 — Testing & Verification

* Vendor adds 2 products (one expiring <7 days, one >15 days).
* Customer1 logs in → can see both products, buy one, see it in purchase history, and only that purchased product in expiry alerts.
* Customer2 logs in → empty history, no alerts until they purchase.
* Multiple browsers/tabs should update in real-time.

---

### 🚀 Deliverable

* A single working project in Replit, previewable at port 5000.
* Bugs fixed, UI improved, Firebase backend integrated.
* Fully functional Vendor + Customer system with real-time sync and new features.
* Ready to deploy to GitHub Pages.
